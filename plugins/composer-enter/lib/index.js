/**
 * Host half of @yfzhou/dsh-composer-enter.
 *
 * The behavior is implemented by the browser client half.
 */
const name = "composer-enter";
const inject = [];

function apply(_ctx) {}

export { apply, inject, name };
